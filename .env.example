VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY

# Optional: OpenAI (gpt-4o-mini) key for dynamic content generation (NEVER commit real key)
VITE_OPENAI_API_KEY=YOUR_OPENAI_KEY

# Copy this file to .env and place your actual Supabase project URL and anon key.
# If you add VITE_OPENAI_API_KEY here it will be exposed to the client build. Prefer using a backend/edge function.
# Do NOT commit real keys if this repository becomes public.
